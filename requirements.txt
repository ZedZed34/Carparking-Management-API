# Framework
Django==5.2.5
djangorestframework==3.15.2
asgiref==3.9.1
sqlparse==0.5.3
tzdata==2025.2

# DB URL + Postgres driver (psycopg v3 works well on Python 3.13)
dj-database-url==2.1.0
psycopg[binary,pool]==3.2.8
# If you must keep the old driver instead, comment the line above and use:
# psycopg2-binary==2.9.9

# Prod server + static files
gunicorn==21.2.0
whitenoise==6.6.0

# CORS / Filters / Health (optional but useful)
django-cors-headers==4.3.1
django-filter==24.2
django-health-check==3.18.1

# Env & security helpers
python-dotenv==1.0.1
django-csp==3.8
# (Removed: django-secure — deprecated, Django has built-in security settings)

# Data/Utils (only if your project uses them)
numpy==2.3.2
pandas==2.3.1
python-dateutil==2.9.0.post0
requests==2.32.4
certifi==2025.8.3
charset-normalizer==3.4.3
urllib3==2.5.0
idna==3.10
six==1.17.0

# Dev tools (optional)
# black==24.10.0
# isort==5.13.2
# flake8==7.1.1
# coverage==7.6.9
