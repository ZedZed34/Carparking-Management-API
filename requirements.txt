# ===================================================================
# Carpark Management API - Dependencies
# ===================================================================
# This single requirements file works for both development and production
# Some dependencies are optional and handled gracefully by the application

# Core Django Framework
Django==5.2.5
djangorestframework==3.15.2
asgiref==3.9.1
sqlparse==0.5.3
tzdata==2025.2

# Data Processing & Analysis
numpy==2.3.2
pandas==2.3.1
python-dateutil==2.9.0.post0
pytz==2025.2

# HTTP Client & Utilities
requests==2.32.4
certifi==2025.8.3
charset-normalizer==3.4.3
urllib3==2.5.0
idna==3.10
six==1.17.0

# Environment Configuration
python-dotenv==1.0.1

# Production Server & Static Files (optional for development)
gunicorn==21.2.0
whitenoise==6.6.0
dj-database-url==2.1.0

# Database Adapters
# PostgreSQL (optional - only needed for production or if using PostgreSQL locally)
# Uncomment the next line if you need PostgreSQL support:
# psycopg2-binary==2.9.9

# Development Tools (optional - uncomment if needed)
# black==24.10.0
# isort==5.13.2
# flake8==7.1.1
# coverage==7.6.9

# ===================================================================
# Installation Notes:
# ===================================================================
# 
# For Development (Windows/Mac/Linux):
#   pip install -r requirements.txt
#   
# For Windows users having issues with psycopg2:
#   1. Skip PostgreSQL: Use SQLite (default) - no extra steps needed
#   2. Or install PostgreSQL separately: pip install psycopg2-binary
#   3. Or use newer alternative: pip install psycopg[binary]
#
# For Production/Docker:
#   All dependencies will be installed automatically
#   PostgreSQL adapter included for production database
# ===================================================================